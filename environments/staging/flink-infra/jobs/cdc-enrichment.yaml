# =============================================================================
# JOB: CDC ENRICHMENT — Staging
# =============================================================================
# parallelism: 3 (between dev:2 and prod:4)
# upgradeMode: savepoint
# bucket: de-platform-staging-flink
# =============================================================================

apiVersion: v1
kind: ConfigMap
metadata:
  name: cdc-enrichment-script
  namespace: flink
  labels:
    app.kubernetes.io/name: cdc-enrichment
    app.kubernetes.io/part-of: de-platform
    environment: staging
data:
  cdc_enrichment.py: |
    """
    CDC Enrichment Processor — Staging

    Same logic as prod. Tests schema evolution and enrichment pipeline
    against the staging Kafka cluster and staging data lake bucket.
    """

    import os
    import logging
    from pyflink.table import EnvironmentSettings, TableEnvironment

    logging.basicConfig(
        level=logging.INFO,
        format="%(asctime)s %(levelname)s [%(name)s] %(message)s",
    )
    log = logging.getLogger("cdc-enrichment")


    def main():
        settings = EnvironmentSettings.in_streaming_mode()
        t_env = TableEnvironment.create(settings)

        cfg = t_env.get_config()
        cfg.set("execution.checkpointing.interval", "30s")
        cfg.set("execution.checkpointing.mode", "EXACTLY_ONCE")
        cfg.set("state.backend", "rocksdb")
        cfg.set("state.backend.incremental", "true")
        cfg.set("state.backend.rocksdb.memory.managed", "true")
        cfg.set(
            "state.checkpoints.dir",
            f"s3://{os.environ['FLINK_S3_BUCKET']}/checkpoints/cdc-enrichment",
        )
        cfg.set(
            "state.savepoints.dir",
            f"s3://{os.environ['FLINK_S3_BUCKET']}/savepoints/cdc-enrichment",
        )
        cfg.set("restart-strategy", "exponential-delay")
        cfg.set("restart-strategy.exponential-delay.initial-backoff", "1s")
        cfg.set("restart-strategy.exponential-delay.max-backoff", "5min")
        cfg.set("restart-strategy.exponential-delay.multiplier", "2.0")

        bootstrap = os.environ["KAFKA_BOOTSTRAP_SERVERS"]
        username = os.environ["KAFKA_USERNAME"]
        password = os.environ["KAFKA_PASSWORD"]
        environment = os.environ.get("ENVIRONMENT", "staging")

        sasl_jaas = (
            "org.apache.kafka.common.security.scram.ScramLoginModule required "
            f'username="{username}" password="{password}";'
        )

        kafka_security = f"""
            'properties.security.protocol'       = 'SASL_SSL',
            'properties.sasl.mechanism'           = 'SCRAM-SHA-512',
            'properties.sasl.jaas.config'         = '{sasl_jaas}',
            'properties.ssl.truststore.location'  = '/opt/flink/certs/kafka-ca.p12',
            'properties.ssl.truststore.type'      = 'PKCS12',
            'properties.ssl.truststore.password'  = 'changeit'
        """

        t_env.execute_sql(f"""
            CREATE TABLE cdc_transactions (
                transaction_id  STRING,
                user_id         STRING,
                amount          DECIMAL(18, 2),
                currency        STRING,
                status          STRING,
                created_at      BIGINT,
                __op            STRING,
                __ts_ms         BIGINT,
                event_time      AS TO_TIMESTAMP_LTZ(__ts_ms, 3),
                WATERMARK FOR event_time AS event_time - INTERVAL '5' SECOND
            ) WITH (
                'connector'                          = 'kafka',
                'topic'                              = 'cdc.postgres-app-db',
                'properties.bootstrap.servers'       = '{bootstrap}',
                'properties.group.id'                = 'flink-cdc-enrichment-job',
                {kafka_security},
                'scan.startup.mode'                  = 'earliest-offset',
                'format'                             = 'json',
                'json.fail-on-missing-field'         = 'false',
                'json.ignore-parse-errors'           = 'true'
            )
        """)

        t_env.execute_sql(f"""
            CREATE TABLE enriched_transactions (
                transaction_id   STRING,
                user_id          STRING,
                amount           DECIMAL(18, 2),
                currency         STRING,
                status           STRING,
                operation_type   STRING,
                source_ts        TIMESTAMP(3),
                processed_at     TIMESTAMP(3),
                environment      STRING,
                PRIMARY KEY (transaction_id) NOT ENFORCED
            ) WITH (
                'connector'                          = 'upsert-kafka',
                'topic'                              = 'enriched.transactions',
                'properties.bootstrap.servers'       = '{bootstrap}',
                {kafka_security},
                'key.format'                         = 'json',
                'value.format'                       = 'json'
            )
        """)

        log.info("Tables defined. Starting CDC enrichment job (staging).")

        stmt = t_env.create_statement_set()
        stmt.add_insert_sql(f"""
            INSERT INTO enriched_transactions
            SELECT
                transaction_id,
                user_id,
                amount,
                currency,
                status,
                CASE __op
                    WHEN 'c' THEN 'INSERT'
                    WHEN 'u' THEN 'UPDATE'
                    WHEN 'r' THEN 'SNAPSHOT'
                    ELSE 'UNKNOWN'
                END                   AS operation_type,
                event_time            AS source_ts,
                NOW()                 AS processed_at,
                '{environment}'       AS environment
            FROM cdc_transactions
            WHERE __op IN ('c', 'u', 'r')
        """)

        stmt.execute().wait()


    if __name__ == "__main__":
        main()

---
apiVersion: flink.apache.org/v1beta1
kind: FlinkDeployment
metadata:
  name: cdc-enrichment
  namespace: flink
  labels:
    app.kubernetes.io/name: cdc-enrichment
    app.kubernetes.io/part-of: de-platform
    environment: staging
spec:
  image: 123456789012.dkr.ecr.ap-south-1.amazonaws.com/flink-platform:1.18.1
  imagePullPolicy: Always
  flinkVersion: v1_18
  serviceAccount: flink-sa

  flinkConfiguration:
    taskmanager.numberOfTaskSlots: "2"
    state.backend: rocksdb
    state.backend.incremental: "true"
    state.backend.rocksdb.memory.managed: "true"
    state.checkpoints.dir: s3://de-platform-staging-flink/checkpoints/cdc-enrichment
    state.savepoints.dir: s3://de-platform-staging-flink/savepoints/cdc-enrichment
    execution.checkpointing.interval: "30000"
    execution.checkpointing.mode: EXACTLY_ONCE
    execution.checkpointing.timeout: "120000"
    execution.checkpointing.min-pause: "15000"
    execution.checkpointing.max-concurrent-checkpoints: "1"
    restart-strategy: exponential-delay
    restart-strategy.exponential-delay.initial-backoff: 1s
    restart-strategy.exponential-delay.max-backoff: 5min
    restart-strategy.exponential-delay.multiplier: "2.0"
    metrics.reporter.prom.factory.class: org.apache.flink.metrics.prometheus.PrometheusReporterFactory
    metrics.reporter.prom.port: "9249"
    s3.access-key: ""
    s3.secret-key: ""
    fs.s3a.aws.credentials.provider: com.amazonaws.auth.WebIdentityTokenCredentialsProvider

  podTemplate:
    spec:
      serviceAccountName: flink-sa
      tolerations:
        - key: dedicated
          operator: Equal
          value: flink
          effect: NoSchedule
      initContainers:
        - name: cert-converter
          image: eclipse-temurin:11-jre-alpine
          command:
            - sh
            - -c
            - |
              keytool -importcert -trustcacerts -noprompt \
                -alias kafka-ca \
                -file /certs-src/ca.crt \
                -keystore /opt/flink/certs/kafka-ca.p12 \
                -storetype PKCS12 \
                -storepass changeit
          volumeMounts:
            - name: kafka-ca-cert
              mountPath: /certs-src
              readOnly: true
            - name: flink-certs
              mountPath: /opt/flink/certs
      volumes:
        - name: cdc-script
          configMap:
            name: cdc-enrichment-script
        - name: kafka-ca-cert
          secret:
            secretName: de-kafka-cluster-cluster-ca-cert
        - name: flink-certs
          emptyDir: {}

  jobManager:
    resource:
      memory: "2048m"
      cpu: 1
    podTemplate:
      spec:
        affinity:
          nodeAffinity:
            requiredDuringSchedulingIgnoredDuringExecution:
              nodeSelectorTerms:
                - matchExpressions:
                    - key: role
                      operator: In
                      values:
                        - flink
        env:
          - name: KAFKA_USERNAME
            valueFrom:
              secretKeyRef:
                name: flink-processor
                key: user
          - name: KAFKA_PASSWORD
            valueFrom:
              secretKeyRef:
                name: flink-processor
                key: password
          - name: KAFKA_BOOTSTRAP_SERVERS
            value: "de-kafka-cluster-kafka-tls-bootstrap.kafka.svc:9093"
          - name: FLINK_S3_BUCKET
            value: "de-platform-staging-flink"
          - name: ENVIRONMENT
            value: "staging"
        volumeMounts:
          - name: cdc-script
            mountPath: /opt/flink/scripts
          - name: flink-certs
            mountPath: /opt/flink/certs

  taskManager:
    resource:
      memory: "6144m"
      cpu: 1
    podTemplate:
      spec:
        affinity:
          nodeAffinity:
            requiredDuringSchedulingIgnoredDuringExecution:
              nodeSelectorTerms:
                - matchExpressions:
                    - key: role
                      operator: In
                      values:
                        - flink
        containers:
          - name: flink-main-container
            resources:
              requests:
                memory: "6Gi"
                cpu: "1"
              limits:
                memory: "8Gi"
                cpu: "2"
        env:
          - name: KAFKA_USERNAME
            valueFrom:
              secretKeyRef:
                name: flink-processor
                key: user
          - name: KAFKA_PASSWORD
            valueFrom:
              secretKeyRef:
                name: flink-processor
                key: password
          - name: KAFKA_BOOTSTRAP_SERVERS
            value: "de-kafka-cluster-kafka-tls-bootstrap.kafka.svc:9093"
          - name: FLINK_S3_BUCKET
            value: "de-platform-staging-flink"
          - name: ENVIRONMENT
            value: "staging"
        volumeMounts:
          - name: cdc-script
            mountPath: /opt/flink/scripts
          - name: flink-certs
            mountPath: /opt/flink/certs

  job:
    jarURI: local:///opt/flink/opt/flink-python_2.12-1.18.1.jar
    entryClass: org.apache.flink.client.python.PythonDriver
    args:
      - "-py"
      - "/opt/flink/scripts/cdc_enrichment.py"
    parallelism: 3       # Between dev (2) and prod (4)
    upgradeMode: savepoint
    state: running
